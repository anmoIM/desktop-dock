# 更新日志

## [1.1] - 2026-09-20

在 1.0 的基础上，这一版加入了收纳区（多面板）与按区分开的设置，并改进了拖拽与文字显示。

**新增**

- **收纳区（多面板）**：每个区一个独立的贴边面板，各有标题、位置、尺寸、外观与呼出热键，可折叠
- **跨区拖动**：把图标拖到另一个区的面板上松手即可换区，拖动时有跟随鼠标的图标虚影
- **隐藏快捷方式小箭头**：取快捷方式所指向程序自身的图标，左下角的小箭头角标消失
- **拖动拖尾特效**：拖动时留下一串逐渐变小、淡出的残影，可开关，长度 10 档可调
- **桌面文件直接拖进面板**：桌面或资源管理器里的文件拖到面板上松手即收录，落在松开的位置
- **设置拆分为两类**：「本区设置」（收纳区 / 外观 / 图标）与「全局设置」（图标与桌面 / 行为），
  外观与图标行为改为按区保存

**改进**

- 图标（含文字）在各自格子里垂直居中；显示文字不再把网格撑大
- 文字紧贴图标下方，过长时自动缩小字号并多折一行
- 文件系统返回偏大位图（如 48px）时统一按设定尺寸缩放，不再压住文字
- 拖动时自由跟随鼠标、**松手才**吸附网格；一格一图标，拖到已占用的格子会把原图标挤到最近的空位
- 新建收纳区时照抄当前区的外观与图标设置

**修复**

- 跨区拖动时图标卡住、虚影留在屏幕上（拖出面板时隐藏磁贴会中断鼠标抓取）
- 关闭设置窗口后折叠失灵（点击标题栏被误判为固定）
- 休眠或分辨率变化后热键叫不出面板
- 右键面板空白处添加的图标没有归入当前收纳区

---

下面按功能给出本版本的完整说明。

### 一、图标收录

- **桌面图标自动收录**：启动即扫描「用户桌面 + 公共桌面」上的快捷方式、文件与文件夹
- **系统图标收录**：此电脑、回收站、控制面板等属于 Shell 命名空间的图标（不是文件），
  通过 COM 枚举获取，并按桌面「查看」里的勾选状态决定收录哪些
- **桌面变动自动收录**：监听桌面目录，新增图标自动进面板、删除自动移除（0.9 秒防抖）
- **手动添加任意路径**：右键面板空白处 → 添加文件或程序 / 添加文件夹，
  可把桌面之外的任何文件收进面板；收进的是右键的那个收纳区，
  选文件期间面板不会收起，添加完会把面板亮出来
- **直接拖进来收录**：把桌面快捷方式或资源管理器里的文件/文件夹直接拖到面板上松手即可，
  图标落在松开的位置；拖到哪个收纳区就归哪个区，之前被移除过的会自动恢复；
  拖动经过面板时面板会高亮并提示「松手收进 XX」
- **移除与恢复**：右键「从面板移除」后进入排除列表，可从托盘菜单一键全部恢复
- **路径失效处理**：文件被删除后自动从面板消失，不留死图标

### 二、收纳区（多面板）

- **多个收纳区**：每个收纳区是一个独立的贴边面板，可分别收纳不同的图标
  （如「常用」「游戏」「工作」），各有自己的标题、位置、尺寸与呼出热键
- **新建 / 重命名 / 删除**：设置 → 收纳区 → 编辑哪个区，可随时增删改；
  删除某个区后，区里的图标自动回到第一个收纳区，不会丢失
- **跨区拖动**：直接把图标拖出面板，拖到另一个收纳区的面板上松手即可换区，
  图标落在松开的位置；拖动时原来的图标留在原地变淡，跟着鼠标走的是图标虚影；
  松手没落在任何面板上就原地不动，不会出现图标卡住或虚影留在屏幕上的情况
- **右键换区**：右键图标 → 移动到收纳区 → 选择目标区，图标自动落到该区空位
- **折叠收纳区**：点标题栏的 ▾ 折叠成一条标题栏，再点 ▸ 展开；
  折叠状态会被记住，适合把不常用的区收起来
- **各自的热键**：每个区可设置自己的呼出热键，留空表示不设
- **互不干扰**：各区独立排列、独立滚动，图标不会重复出现在两个区里

### 三、图标的显示与定制

- **重命名**（右键 → 重命名）：只改面板上显示的名字，不动原文件
- **更换图标**（右键 → 更换图标）：支持图片文件（ico/png/jpg/bmp/gif/webp）
  和程序图标（exe/dll/lnk），同样是映射，不改原文件
- **恢复默认**：一键还原原始名称与图标
- **隐藏快捷方式小箭头**：开启后取快捷方式所指向程序自身的图标，
  图标左下角的小箭头角标消失（.lnk 与 .url 均支持，解析结果带缓存）
- 以上映射按路径记录，文件移动后自动失效，不会串到别的图标上

### 四、排列方式

- **自由拖动**：按住图标拖到任意位置
- **网格对齐**：拖动过程中图标自由跟随鼠标，**松手时**才吸附到最近格点
  （超过半格换格、半格内弹回），拖动时不会一格格地跳
- **自动排序**：始终按顺序铺满网格；一旦手动拖动即自动退出该模式
- **位置持久化**：手动摆放的位置会记住，重新扫描、改尺寸后依然保留
- **一格一图标**：手动摆过的位置优先落位，其余图标只补空格子，
  图标不会重复叠放在同一格；原来那格被占用时自动挪到最近的空格子
- **拖动挤位**：把图标拖到已有图标的格子上，被压住的图标会被挤到最近的空位
- **拖动拖尾特效**：拖动时留下一串逐渐变小、淡出的残影，跟着鼠标走，
  跨收纳区拖动也看得到；可在设置里开关，长度 10 档可调
- **滚动查看**：图标放不下时滚轮滚动，内侧显示位置指示条
- **一键重置**：托盘菜单「重置图标位置」

### 五、外观

- **面板**：淡蓝色半透明圆角面板，透明度可调（20%~100%）
- **背景色**：RGB 三原色自由调节，保留自上而下的渐变质感
- **面板标题**：可自定义（默认「桌面收录」）
- **图标大小**：24~72 px
- **文字**：字号 7~18 pt 可调，紧贴图标下方，始终留在自己格子里
- **文字自适应**：名字放不下时自动缩小字号（最小 6 pt）、并按格子剩余高度多折一行，
  大多数长名字都能完整显示；实在放不下才用省略号收尾
- **图标尺寸统一**：文件夹、txt/docx 等文件系统有时会返回偏大的位图（如 48px），
  现在统一按设定尺寸缩放，不会再压住图标下方的文字
- **网格不随文字变大**：显示文字时的格子宽度与不显示时完全一致，
  只按文字行数增加行距；图标（含文字）在各自格子里垂直居中
- **文字颜色**：内置黑、白、红、橙、黄、绿、蓝、淡蓝、紫 9 色，默认黑色
- **独立透明度**：图标透明度与文字透明度可分别设置，与面板背景互不影响
- **每个区各调各的**：透明度、背景色、图标大小、文字大小与颜色等外观项都按收纳区保存，
  改了「工具」区不会动到「游戏」区
- **文字开关**：可隐藏图标下方文字；隐藏时鼠标悬停 0.4 秒显示名称提示

### 六、呼出与折叠

- **全局热键**：每个收纳区各自一个热键，点输入框直接按键盘录制，
  支持 Ctrl/Alt/Shift/Win 任意组合或单键；
  默认键被其他软件占用时自动切换到备选键并提示
- **屏幕边缘悬停**：鼠标移到所停靠一侧的边缘即滑出；采用主动轮询光标判断
  （不依赖窗口鼠标事件），因此桌面窗口或最大化窗口压在最前面也照样能呼出
- **停留判定**：边缘停留约 180ms 才展开，避免鼠标扫过误触
- **隐形检测圈**：面板四周 18px 内都算「还在面板上」，擦边不会误收起
- **快速收起**：鼠标离开约 100ms 后判定、120ms 完成收起动画
- **不打断操作**：右键菜单、设置窗口打开期间不会自动收起
- **全屏保护**：可选「前台有全屏或最大化窗口时不要弹出」，游戏/观影时不打扰

### 七、窗口与位置

- **拖动移动**：按住标题栏拖动面板，松手后自动贴向最近的一侧，
  收起位置也随之切换到该侧
- **尺寸可调**：拖动朝向屏幕中央的边调宽、拖动底边调高；也可在设置里精确指定
- **自动适应**：宽度/高度都支持「自动适应图标数量」
- **位置记忆**：纵向位置与尺寸都会保存

### 八、系统集成

- **隐藏系统桌面图标**：等同于桌面右键「查看 → 显示桌面图标」，
  开启后运行期间持续保持（资源管理器重启也会重新隐藏），
  关闭设置或退出程序时自动还原
- **任务栏背景透明**：只改任务栏背景的合成效果，最小化应用图标、
  时钟、网络状态等全部照常显示；不写注册表、不重启资源管理器
- **开机自启动**：写入当前用户启动项，无需管理员权限
- **托盘菜单**：收纳区（逐个显示/隐藏、进设置管理）、重新扫描、重置位置、
  恢复移除项、窗口置顶、开机自启、隐藏桌面图标、全屏不弹出、设置、退出
- **显示桌面兼容**：按 Win+D 不会把面板最小化；若被系统最小化、隐藏或挪走，
  700ms 内自动恢复尺寸与位置
- **单实例运行**：重复启动不会开出第二个面板

### 九、设置项总览

设置界面分成「本区设置」和「全局设置」两个选项卡：

| 选项卡 | 分组 | 设置项 |
|---|---|---|
| 本区设置 | 收纳区 | 编辑哪个区（切换/新建/重命名/删除）、标题、停靠位置、呼出热键、宽度（可自动）、高度（可自动） |
| 本区设置 | 外观 | 透明度、图标大小、文字大小、图标透明度、文字透明度、文字颜色、背景 R/G/B |
| 本区设置 | 图标 | 在图标下方显示文字、拖动时自动对齐网格、自动排序、拖动拖尾特效（可开关 + 长度 10 档） |
| 全局设置 | 图标与桌面 | 隐藏快捷方式小箭头、隐藏系统桌面图标 |
| 全局设置 | 行为 | 窗口置顶、鼠标移到屏幕边缘自动滑出、前台全屏/最大化时不弹出、任务栏背景透明、开机自动启动 |

- 「本区设置」只作用于上面选中的那个收纳区（可逐个区切换着调），
  外观与图标行为现在也按区独立，例如给「游戏」区放大图标、给「工作」区隐藏文字
- 「全局设置」是所有收纳区共用的开关
- 新建收纳区时会照抄当前区的这套外观与图标设置，省得再调一遍

### 十、交付形式

- **主程序**：`桌面收录.exe`，单文件、免 Python 环境，无控制台窗口
- **安装程序**：`桌面收录-安装程序.exe`，运行后选择安装目录，
  自动释放程序文件、生成配置并创建桌面快捷方式（已有配置不覆盖）
- 配置保存在程序同目录的 `data/config.json`，卸载只需删除目录与快捷方式

## [1.0] - 2026-09-20

首个开源版本：单个半透明贴边面板收录桌面图标，支持重命名与更换图标、
拖动排列与网格对齐、热键与边缘悬停呼出、隐藏系统桌面图标、任务栏背景透明、
开机自启，以单文件 exe 与安装程序交付。